<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/public/output.css" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full p-8 min-h-screen bg-zinc-900">
        <div class="flex w-full justify-start">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block text-white" href="/logout">Logout</a>
        </div>
        <h5 class="mb-5 font-light text-amber-50">edit your post</h5>
        <form method="post" action="/update/<%= post._id %>">
            <textarea placeholder="What's in your mind"
                class="p-3 outline-none w-1/3 resize-none text-white bg-transparent border-2 border-zinc-800 rounded-md"
                name="content"><%= post.content %></textarea>
            <input class="px-3 w-40 text-sm py-2 bg-yellow-500 text-black block rounded-md mt-2" type="submit"
                value="Update post" />
        </form>
    </div>

    <script src="/public/script.js"></script>
</body>

</html>