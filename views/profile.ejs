<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/public/output.css" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full p-8 min-h-screen bg-zinc-900">
        <div class="flex w-full justify-start">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block text-white" href="/logout">Logout</a>
        </div>
        <h3 class="text-3xl text-white">
            <span class="" font-light>Hello</span> , <%= user.name %>
        </h3>
        <h5 class="mb-5 font-light text-amber-50">you can create a new post.</h5>
        <form method="post" action="/post">
            <textarea placeholder="What's in your mind"
                class="p-3 outline-none w-1/3 resize-none text-white bg-transparent border-2 border-zinc-800 rounded-md"
                name="content"></textarea>
            <input class="px-3 w-40 text-amber-100 text-sm py-2 bg-blue-500 block rounded-md mt-2" type="submit"
                value="create new post" />
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts.</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 mb-2 p-4 rounded-md bg-zinc-800 border-2 border-zinc-700">
                        <h4 class="text-blue-500 mb-2">
                            @<%= user.username %>
                        </h4>
                        <p class="tracking-tight mb-2 text-zinc-100 font-light">
                            <%= post.content %>
                        </p>
                        <small class="mt-2  text-zinc-100 inline-block"><%= post.likes.length %> likes</small>
                        <div class="" btns flex>
                            <a class="text-blue-500 pr-1" href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %></a>
                            <a class="text-zinc-400" href="/edit/<%= post._id %>">edit</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>

    <script src="/public/script.js"></script>
</body>

</html>